# Lokale Import-Dateien mit echten Spielerdaten duerfen nie ins Repo
# (wird ueber GitHub Pages oeffentlich ausgeliefert, s. Datenimport in app.js)
*import*.json
seed-data.*

# Lokales Claude-Projektgedaechtnis, nicht Teil des oeffentlichen Repos
CLAUDE.md
